@model FM.GeoLocation.Contract.Models.LookupAddressResponse
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome - @Model.GeoLocationDto?.Address</h1>
    <p>We believe you are from @Model.GeoLocationDto?.CityName, @Model.GeoLocationDto?.CountryName</p>

    <div class="col-sm-12">
        <div id="map" style="height: 450px; width: 100%;"></div>
    </div>
</div>

@section Scripts {
    <script>
        function initMap() {
            var myLatLng = { lat: @Model.GeoLocationDto?.Latitude, lng: @Model.GeoLocationDto?.Longitude };

            var map = new google.maps.Map(document.getElementById('map'),
                {
                    zoom: 4,
                    center: myLatLng
                });

            var marker = new google.maps.Marker({
                position: myLatLng,
                map: map,
                title: '@Model.GeoLocationDto?.Address Location'
            });
        }
    </script>

    <script async defer src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDqX0zqt8PWTqRtk0PA6luVuOJiWSgbiw0&signed_in=false&callback=initMap"></script>
}

